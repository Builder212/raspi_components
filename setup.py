from setuptools import setup,find_packages

setup(
    name = "dht",
    version = "V1.0.0",
    description = "Read and return data from the DHT11 module with the raspberry pi.",
    author = "Builder212",
    license = "MIT",
    packages = find_packages(),
    scripts = ["dht.py"],
    )

#come back to this later.
